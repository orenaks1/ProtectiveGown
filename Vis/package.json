{
  "name": "hmi",
  "version": "1.0.0",
  "description": "",
  "productName": "PPE",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",

    "package-win": "electron-packager . PPE-Machine --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Electron Tutorial App\""  
  },


  "author": "oren",
  "license": "ISC",
  "devDependencies": {
    "electron": "^11.5.0",
    "electron-builder": "^22.4.1",
    "electron-packager": "^14.2.1"
  },
  "dependencies": {
    "node-ads": "^1.5.0"
  }




  
}
